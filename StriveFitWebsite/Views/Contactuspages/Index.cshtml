@model IEnumerable<StriveFitWebsite.Models.Contactuspage>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/AdminLayout.cshtml";

}
<div class="content">
    <h1>Index</h1>

    <p>
        <a asp-action="Create">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                    @Html.DisplayNameFor(model => model.Title)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.Contentvalue)
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
    @foreach (var item in Model) {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.Title)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Contentvalue)
                </td>
                <td>
                    <a asp-action="Edit" asp-route-id="@item.Pageid">Edit</a> |
                    <a asp-action="Details" asp-route-id="@item.Pageid">Details</a> |
                    <a asp-action="Delete" asp-route-id="@item.Pageid">Delete</a>
                </td>
            </tr>
    }
        </tbody>
    </table>
</div>